module.exports = {
    siteUrl: `https://www.chazkondo.com`, // Site domain. Do not include a trailing slash!

    postsPerPage: 9, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Chaz Kondo - Developer`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Software Engineer Profile + Extras`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Chaz Kondo`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `myfavicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#fff`, // Used for Offline Manifest
    themeColor: `#fff`, // Used for Offline Manifest
}
